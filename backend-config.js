// Aither Backend configuration for Aither Notes.
// Keep the backend URL in one place so GitHub Pages and desktop builds use the same API.
// Set window.AITHER_BACKEND_URL before app.js loads if your deployment uses another endpoint.
window.AITHER_BACKEND_URL = window.AITHER_BACKEND_URL || 'https://aitherbackend.onrender.com';
